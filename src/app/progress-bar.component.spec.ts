import { ProgressBarDirective } from './progress-bar.component';

describe('ProgressBarDirective', () => {
  it('should create an instance', () => {
    const directive = new ProgressBarDirective();
    expect(directive).toBeTruthy();
  });
});
